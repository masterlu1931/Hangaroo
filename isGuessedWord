def getGuessedWord(secretWord,lettersGuessed):
    secretWord='apple'
    GuessedWord = secretWord
    for x in GuessedWord:
        continue
    else:
        GuessedWord=GuessedWord.replace(x,' ')
        return GuessedWord
